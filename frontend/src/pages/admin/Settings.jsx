import {useEffect,useState} from 'react'
import api from '../../api'
const defaults={siteName:'',tagline:'',contactEmail:'',logoText:'',footerText:'',defaultSeoTitle:'',defaultSeoDescription:'',googleAnalyticsId:'',brandColor:'#17344e',allowComments:false,maintenanceMode:false,customCss:'',customHead:''}
export default function Settings(){const [s,setS]=useState(defaults);const [message,setMessage]=useState('');useEffect(()=>{api.get('/settings').then(r=>setS({...defaults,...r.data}))},[]);function set(key,value){setS({...s,[key]:value})}async function save(e){e.preventDefault();await api.put('/admin/settings',s);setMessage('Settings saved')}return <form className="settings-panel" onSubmit={save}><div className="admin-title"><div><h1>Advanced settings</h1><p>Control identity, search defaults, analytics, accessibility, and custom presentation.</p></div><button className="primary">Save settings</button></div>{message&&<div className="alert">{message}</div>}<div className="settings-grid"><section className="panel"><h3>Publication identity</h3><label>Site name<input value={s.siteName} onChange={e=>set('siteName',e.target.value)}/></label><label>Tagline<input value={s.tagline} onChange={e=>set('tagline',e.target.value)}/></label><label>Logo text<input value={s.logoText} onChange={e=>set('logoText',e.target.value)}/></label><label>Contact email<input type="email" value={s.contactEmail} onChange={e=>set('contactEmail',e.target.value)}/></label><label>Footer text<textarea rows="3" value={s.footerText} onChange={e=>set('footerText',e.target.value)}/></label></section><section className="panel"><h3>Search and social defaults</h3><label>Default SEO title<input value={s.defaultSeoTitle||''} onChange={e=>set('defaultSeoTitle',e.target.value)}/></label><label>Default SEO description<textarea rows="4" value={s.defaultSeoDescription||''} onChange={e=>set('defaultSeoDescription',e.target.value)}/></label><label>Google Analytics ID<input placeholder="G-XXXXXXXXXX" value={s.googleAnalyticsId||''} onChange={e=>set('googleAnalyticsId',e.target.value)}/></label><label>Brand color<input type="color" value={s.brandColor||'#17344e'} onChange={e=>set('brandColor',e.target.value)}/></label></section><section className="panel"><h3>Editorial controls</h3><label className="checkbox"><input type="checkbox" checked={!!s.allowComments} onChange={e=>set('allowComments',e.target.checked)}/> Allow comments on future articles</label><label className="checkbox"><input type="checkbox" checked={!!s.maintenanceMode} onChange={e=>set('maintenanceMode',e.target.checked)}/> Maintenance mode</label><p className="field-note">Maintenance mode is stored as a site setting and can be connected to deployment middleware when the public site needs to be paused.</p></section><section className="panel"><h3>Custom code</h3><label>Head markup<textarea rows="5" value={s.customHead||''} onChange={e=>set('customHead',e.target.value)} placeholder="Verification tags or approved analytics snippets"/></label><label>Custom CSS<textarea rows="7" value={s.customCss||''} onChange={e=>set('customCss',e.target.value)}/></label></section></div></form>}

import {useEffect,useState} from 'react';import api from '../../api'
export default function Users(){const [items,setItems]=useState([]);const load=()=>api.get('/admin/users').then(r=>setItems(r.data));useEffect(()=>{load()},[])
async function create(e){e.preventDefault();const f=new FormData(e.currentTarget);await api.post('/admin/users',Object.fromEntries(f));e.currentTarget.reset();load()}
return <div><div className="admin-title"><div><h1>Admins & Authors</h1><p>Create multiple accounts with role-based access.</p></div></div><div className="two-col"><form className="panel" onSubmit={create}><h3>Create account</h3><label>Name<input name="name" required/></label><label>Email<input type="email" name="email" required/></label><label>Password<input type="password" name="password" minLength="8" required/></label><label>Role<select name="role"><option>AUTHOR</option><option>EDITOR</option><option>ADMIN</option><option>SUPER_ADMIN</option></select></label><button className="primary">Create account</button></form><div className="panel"><h3>Accounts</h3>{items.map(u=><div className="list-row" key={u.id}><div><strong>{u.name}</strong><small>{u.email}</small></div><span className="badge">{u.role}</span></div>)}</div></div></div>}
